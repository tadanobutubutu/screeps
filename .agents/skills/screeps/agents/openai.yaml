interface:
  display_name: "Screeps"
  short_description: "Repo-specific patterns and workflows for screeps"
  default_prompt: "Use the screeps repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true