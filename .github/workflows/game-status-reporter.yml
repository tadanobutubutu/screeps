name: 📊 Game Status (Legacy - Disabled)

# This workflow is disabled in favor of GitHub Sync mode
# See game-monitor-15min.yml for current status monitoring

on:
  workflow_dispatch:  # Manual only

permissions:
  contents: write

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    name: Legacy API Mode (Disabled)
    
    steps:
      - name: Notice
        run: |
          echo "ℹ️ This workflow is disabled"
          echo "✅ Using GitHub Sync mode instead (API-free)"
          echo "🔗 Screeps syncs directly from this repository"
          echo ""
          echo "Benefits of GitHub Sync:"
          echo "  - No API tokens needed"
          echo "  - Instant deployment"
          echo "  - Simpler setup"
          echo "  - Zero API rate limits"
          echo ""
          echo "To check game status:"
          echo "  1. Use Screeps in-game console"
          echo "  2. Check Memory.logs for errors"
          echo "  3. Auto-fix system monitors errors"
