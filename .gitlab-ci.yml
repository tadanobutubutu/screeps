image: node:20

cache:
  paths:
    - node_modules/

before_script:
  - npm install -g pnpm
  - pnpm install --frozen-lockfile

stages:
  - test
  - quality

run_tests:
  stage: test
  script:
    - pnpm run test

run_lint:
  stage: quality
  script:
    - pnpm run lint
