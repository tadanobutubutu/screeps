# CxFlow workflow disabled - Checkmarx secrets not configured for this repo
# This file is intentionally left as a placeholder to prevent Jules from re-adding it
# Original: checkmarx-ts/checkmarx-cxflow-github-action

name: CxFlow

on:
  workflow_dispatch: # manual only, never runs automatically

# Minimal permissions (read-only) as required by security best practices
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo 'CxFlow is disabled - Checkmarx secrets not configured'
