name: 🏷️ Auto-Label Issues

on:
  issues:
    types: [opened, edited]

permissions:
  issues: write

jobs:
  auto-label:
    runs-on: ubuntu-latest
    name: Automatically Label Issues
    
    steps:
      - name: Analyze and Label Issue
        run: |
          echo "🏷️ Analyzing issue..."
          
          ISSUE_NUMBER=${{ github.event.issue.number }}
          
          LABELS=""
          
          # Use jq to safely parse JSON and check for patterns
          gh issue view $ISSUE_NUMBER --json title,body | jq -r '.title + " " + .body' > content.txt
          
          # Detect issue type from content
          if grep -qi 'error\|exception\|crash\|fail' content.txt; then
            LABELS="$LABELS,bug,error"
            echo "🐛 Detected: Bug/Error"
          fi
          
          if grep -qi 'null\|undefined\|cannot read property\|cannot convert' content.txt; then
            LABELS="$LABELS,null-error"
            echo "⚠️ Detected: Null/Undefined error"
          fi
          
          if grep -qi 'performance\|slow\|lag\|timeout\|cpu' content.txt; then
            LABELS="$LABELS,performance"
            echo "⚡ Detected: Performance issue"
          fi
          
          if grep -qi 'feature\|enhancement\|improve\|add' content.txt; then
            LABELS="$LABELS,enhancement"
            echo "✨ Detected: Feature request"
          fi
          
          if grep -qi 'documentation\|docs\|readme\|wiki' content.txt; then
            LABELS="$LABELS,documentation"
            echo "📚 Detected: Documentation"
          fi
          
          if grep -qi 'security\|vulnerability\|exploit\|unsafe' content.txt; then
            LABELS="$LABELS,security"
            echo "🔒 Detected: Security issue"
          fi
          
          if grep -qi 'workflow\|action\|ci\|cd\|github' content.txt; then
            LABELS="$LABELS,workflow"
            echo "🔧 Detected: Workflow issue"
          fi
          
          if grep -qi 'how to\|how do\|question\|help' content.txt; then
            LABELS="$LABELS,question"
            echo "❓ Detected: Question"
          fi
          
          if grep -qi 'urgent\|critical\|asap\|important' content.txt; then
            LABELS="$LABELS,urgent"
            echo "🔴 Detected: Urgent"
          fi
          
          if grep -qi 'syntax\|typo\|formatting\|null\|undefined' content.txt; then
            LABELS="$LABELS,auto-fix"
            echo "🤖 Detected: Auto-fixable"
          fi
          
          # Remove leading comma
          LABELS=$(echo "$LABELS" | sed 's/^,//')
          
          if [ -n "$LABELS" ]; then
            echo "🏷️ Adding labels: $LABELS"
            
            # Convert comma-separated to array and add labels
            IFS=',' read -ra LABEL_ARRAY <<< "$LABELS"
            for label in "${LABEL_ARRAY[@]}"; do
              gh issue edit $ISSUE_NUMBER --add-label "$label" 2>/dev/null || echo "Label $label not found (will be created)"
            done
            
            echo "✅ Labels added"
          else
            echo "ℹ️ No automatic labels detected"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
      
      - name: Add Auto-Fix Comment
        if: contains(github.event.issue.title, 'error') || contains(github.event.issue.title, 'Error')
        run: |
          ISSUE_NUMBER=${{ github.event.issue.number }}
          
          # Check if auto-fix comment already exists
          EXISTING=$(gh issue view $ISSUE_NUMBER --json comments --jq '.comments[].body' | grep -c "Auto-Fix System" || echo "0")
          
          if [ "$EXISTING" = "0" ]; then
            gh issue comment $ISSUE_NUMBER --body "🤖 **Auto-Fix System**
            
            This issue appears to be an error or bug. The auto-fix system will analyze it and attempt to create an automatic fix.
            
            ## What happens next?
            
            1. 🔍 Issue will be analyzed
            2. 🤖 Automatic fix will be attempted
            3. 🔄 Pull request will be created
            4. 👀 Review and merge the fix
            
            You can also manually trigger the fix by adding the \`auto-fix\` label.
            
            ---
            
            *Automated by Auto-Label System*"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
