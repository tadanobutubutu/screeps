version: 2.1

jobs:
  noop:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: No-op
          command: echo "CircleCI is configured. No CI tasks defined yet."

workflows:
  ci:
    jobs:
      - noop
