# PR Labeler configuration - automatically label PRs based on file changes

# Documentation
documentation:
  - any: ['**/*.md', 'docs/**/*', 'wiki/**/*']

# GitHub Actions
github-actions:
  - any: ['.github/workflows/**/*', '.github/actions/**/*']

# Dependencies
dependencies:
  - any: ['package.json', 'package-lock.json', 'yarn.lock', 'pnpm-lock.yaml']

# JavaScript
javascript:
  - any: ['**/*.js', '**/*.mjs', '**/*.cjs']

# Configuration
config:
  - any: ['**/*.json', '**/*.yml', '**/*.yaml', '**/*.toml', '.gitignore', '.gitattributes']

# Tests
test:
  - any: ['**/*.test.js', '**/*.spec.js', 'test/**/*', 'tests/**/*']

# CI/CD
ci:
  - any: ['.github/**/*', '.gitlab-ci.yml', '.circleci/**/*', 'azure-pipelines.yml']

# Security
security:
  - any: ['SECURITY.md', '.github/workflows/codeql.yml', '.github/workflows/secret-scanning.yml', '.gitleaks.toml']

# Bot-related
bot:
  - any: ['.github/dependabot.yml', '.github/renovate.json', '.github/mergify.yml', '.all-contributorsrc']
