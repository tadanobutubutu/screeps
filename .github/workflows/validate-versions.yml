name: Validate Versions

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/**'
      - '**/package.json'
  pull_request:
    branches:
      - main
    paths:
      - '**/package.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Done"
